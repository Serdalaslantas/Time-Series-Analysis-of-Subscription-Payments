This notebook analyzes the time series of subscription payments by the following models: ARIMA,SARIMA,PROPHET, LSTM, GRU.

You need to install the necessary libraries first!

!pip install -r requirements.txt

